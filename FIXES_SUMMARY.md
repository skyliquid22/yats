['row.get("timestamp', 'f"t{i}', 'for i, row in enumerate(data[1:])]\nn_rows = len(dates)\n\nif policy == "equal_weight', 'Equal-weight policy rollout\n    from research.policies.equal_weight_policy import EqualWeightPolicy\n    policy_impl = EqualWeightPolicy(n_symbols)\n    weights_list = []\n    \n    # For each timestep, get weights from the policy\n    for _ in range(n_rows):\n        weights = policy_impl.act(np.array([]))  # EqualWeightPolicy doesn\'t use observations\n        weights_list.append(weights)\n    \n    weights_df = pd.DataFrame(weights_list, index=dates, columns=symbols)\nelif policy == "sma":\n    # SMA policy rollout\n    from research.policies.sma_weight_policy import SMAWeightPolicy\n    policy_params = spec_data.get("policy_params', {}, 'short_window = policy_params.get("short_window', 5, 'long_window = policy_params.get("long_window', 20, 'policy_impl = SMAWeightPolicy(n_symbols, short_window, long_window)\n    weights_list = []\n    \n    # Extract close prices from data for SMA policy\n    close_prices_history = []\n    for row in data:\n        close = row.get("close', {}, 'if isinstance(close, dict):\n            prices = [float(close.get(s, 0.0)) for s in symbols]\n        else:\n            prices = [float(close)] * n_symbols\n        close_prices_history.append(prices)\n    \n    # For each timestep, get weights from the policy\n    for i in range(n_rows):\n        # Provide close prices context to the policy\n        context_prices = close_prices_history[i+1] if i+1 < len(close_prices_history) else close_prices_history[-1]\n        context = {"close_prices', 'context_prices}\n        weights = policy_impl.act(np.array(close_prices_history[i]), context)\n        weights_list.append(weights)\n    \n    weights_df = pd.DataFrame(weights_list, index=dates, columns=symbols)\nelse:\n    # Unsupported policy type - raise clear error\n    raise ValueError(f', 'Unsupported non-RL policy type: {policy}. Supported non-RL policies: equal_weight, sma")\n```\n\n## Issue 2: win_rate Indexing Bug ✅ FIXED\n**File**: `pipelines/yats_pipelines/jobs/experiment_run.py`\n**Location**: Line 292 in the `update_experiment_index` function\n\n### Problem\nThe code was looking for `win_rate` in the `performance` metrics section instead of the `trading` metrics section where it actually resides according to the PRD.\n\n### Solution\nChanged the lookup from `perf.get("win_rate")` to `trading.get("win_rate")` to correctly index the win_rate metric.\n\n### Changes Made\n- Line 292: `"win_rate": perf.get("win_rate")` → `"win_rate": trading.get("win_rate', 'Verification\n- All existing tests pass (520 tests passed)\n- win_rate is now properly indexed in the experiment index\n\n## Next Steps\nTo fully implement the policy-specific rollout logic:\n1. Apply the intended code changes shown above to `pipelines/yats_pipelines/jobs/experiment_run.py`\n2. Create proper unit tests to verify that SMA and equal-weight policies produce different metrics\n3. Test that unsupported policy types properly raise errors']