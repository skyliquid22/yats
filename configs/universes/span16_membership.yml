# Generated by scripts/build_pit_universe.py — do not edit by hand;
# re-run the builder instead (requires APCA_API_KEY_ID /
# APCA_API_SECRET_KEY in the environment):
#   PYTHONPATH=.:pipelines uv run python scripts/build_pit_universe.py
# POINT-IN-TIME: each rebalance uses only bars dated on or before the
# rebalance date; the candidate pool includes delisted (inactive)
# assets, so there is no survivorship bias.
name: pit250
description: point-in-time top 250 US common stocks by median daily dollar volume;
  quarterly rebalance; 90d trailing screen ending at each rebalance date; active +
  delisted candidate pool (no survivorship bias)
generated_at: 2026-09-16 19:38:37 UTC
method:
  ranking: median daily dollar volume (close * volume)
  window_days: 90
  min_bars_in_window: 20
  top_n: 250
  tie_break: alphabetical
  rebalance: quarterly (calendar quarter starts)
  pit_rule: bars with session date <= rebalance date only
rebalance_dates:
  first: '2016-01-04'
  last: '2026-07-01'
  count: 43
candidate_pool:
  assets_active: 14338
  assets_inactive: 19178
  after_common_stock_filter: 6781
  with_bar_history: 6739
candidate_filter: 'us_equity assets (active + inactive) filtered to plausible common
  stock: 1-5 uppercase letters (no ''.'', ''/'', digits), no NASDAQ W/R/U 5th-letter
  suffixes, name heuristics reject warrants/rights/units/preferred/debt/escrow/funds,
  ETF denylist, OTC excluded; see research/universe/pit.py'
bar_history:
  start: '2015-09-29'
  end: '2026-09-15'
  cache: .yats_data/pit_cache/ (gitignored, per-symbol parquet)
artifact:
  path: configs/universes/span16_membership.parquet
  rows: 10500
  size_bytes: 119835
  columns:
  - rebalance_date
  - symbol
  - rank
  - median_dollar_volume
  commit_policy: committed only if under 5 MB; otherwise regenerate with the builder
    command above
