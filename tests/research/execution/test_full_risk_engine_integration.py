[['AAPL', 'MSFT'], ['Signal(\n                timestamp=datetime(2024, 6, 15, tzinfo=timezone.utc),\n                symbol="AAPL', 'target_position_pct=0.3,  # 30% of NAV\n                model_version="test-v1', 'Signal(\n                timestamp=datetime(2024, 6, 15, tzinfo=timezone.utc),\n                symbol="MSFT', 'target_position_pct=0.2,  # 20% of NAV\n                model_version="test-v1', 'loop.add_signals(signals)\n\n        # Mock state methods\n        loop._position_writer.positions = {}\n        loop._get_current_prices = MagicMock(return_value={"AAPL', 150.0, 'MSFT": 300.0})\n        loop._compute_nav = MagicMock(return_value=100000.0)  # $100K NAV\n        loop._portfolio_writer.write_snapshot.return_value = MagicMock(drawdown=-0.01)\n        \n        # Mock risk engine to return pass decisions\n        with patch("research.execution.paper_trading.project_weights_full', 'as mock_project:\n            mock_project.return_value = RiskResult(\n                weights=np.array([0.3, 0.2]),  # Same weights (no adjustment)\n                decisions=[\n                    RiskDecision(\n                        rule_id="max_gross_exposure', 'decision=Decision.PASS,\n                        details={"total', 0.5, 'limit', 1.0], [0], [0.3, 0.2], [0.3, 0.2], ['Signal(\n                timestamp=datetime(2024, 6, 15, tzinfo=timezone.utc),\n                symbol="AAPL', 'target_position_pct=0.4,  # 40% of NAV - might be rejected\n                model_version="test-v1', 'loop.add_signals(signals)\n\n        # Mock state methods\n        loop._position_writer.positions = {}\n        loop._get_current_prices = MagicMock(return_value={"AAPL": 150.0})\n        loop._compute_nav = MagicMock(return_value=100000.0)\n        loop._portfolio_writer.write_snapshot.return_value = MagicMock(drawdown=-0.01)\n        \n        # Mock risk engine to return REJECT decision\n        with patch("research.execution.paper_trading.project_weights_full', 'as mock_project:\n            mock_project.return_value = RiskResult(\n                weights=np.array([0.4]),  # Same weights \n                decisions=[\n                    RiskDecision(\n                        rule_id="max_gross_exposure', 'decision=Decision.REJECT,\n                        details={"total', 0.4, 'limit', 0.3], ['Signal(\n                timestamp=datetime(2024, 6, 15, tzinfo=timezone.utc),\n                symbol="AAPL', 'target_position_pct=0.5,  # High exposure that might trigger halt\n                model_version="test-v1', 'loop.add_signals(signals)\n\n        # Mock state methods\n        loop._position_writer.positions = {}\n        loop._get_current_prices = MagicMock(return_value={"AAPL": 150.0})\n        loop._compute_nav = MagicMock(return_value=100000.0)\n        loop._portfolio_writer.write_snapshot.return_value = MagicMock(drawdown=-0.01)\n        \n        # Mock risk engine to return HALT decision\n        with patch("research.execution.paper_trading.project_weights_full', 'as mock_project:\n            mock_project.return_value = RiskResult(\n                weights=np.array([0.5]),\n                decisions=[\n                    RiskDecision(\n                        rule_id="max_gross_exposure', 'decision=Decision.HALT,\n                        details={"total', 0.5, 'limit', 0.3], ['Signal(\n                timestamp=datetime(2024, 6, 15, tzinfo=timezone.utc),\n                symbol="AAPL', 'target_position_pct=0.1,  # Small position\n                model_version="test-v1', 'loop.add_signals(signals)\n\n        # Mock state methods\n        loop._position_writer.positions = {"AAPL": MagicMock()}\n        loop._get_current_prices = MagicMock(return_value={"AAPL": 150.0})\n        loop._compute_nav = MagicMock(return_value=100000.0)\n        loop._portfolio_writer.write_snapshot.return_value = MagicMock(drawdown=-0.01)\n        \n        # Mock risk engine to return pass decisions (which should still be logged)\n        with patch("research.execution.paper_trading.project_weights_full', 'as mock_project:\n            mock_project.return_value = RiskResult(\n                weights=np.array([0.1]),\n                decisions=[\n                    RiskDecision(\n                        rule_id="max_gross_exposure', 'decision=Decision.PASS,\n                        details={"total', 0.1, 'limit', 1.0]]